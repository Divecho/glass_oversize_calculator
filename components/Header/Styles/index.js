import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    header: {
        fontSize: 21,
        color: '#3893e5',
        fontWeight: 'bold',
        paddingVertical: 12,
    },
});

export default styles;